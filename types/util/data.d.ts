export declare function getObject(
    params: Record<string, any>,
    options: {
        alias?: boolean,
        page?: boolean,
        find?: boolean,
        norm?: boolean
    }
): Record<string, any>;
